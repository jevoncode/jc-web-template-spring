0.0.1 
1. 搭建spring最基本的依赖注入和aop的模块
2. 整合spring和slf4j
	2-1. log4j.additivity.com.jc=true  //true则表明不单只在子logger输出，还会在父log输出。false则表明只在子log输出。
3. test文件夹下com.jc.web.test包里有测试spring的模块是否可用
4. 暂未加入数据层模块和webMVC模块,在这个0.0.1大家可以选择喜欢的模块添加上去与spring整合.